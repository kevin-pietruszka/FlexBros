import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flexbros.app',
  appName: 'FlexBros',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
